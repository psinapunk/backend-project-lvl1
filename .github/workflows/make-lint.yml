name: make-lint

on: push

jobs:
  build:
    runs-on: ubuntu-latest
    steps: 
      - uses: actions/checkout@v2
      - uses: actions/setup-eslint@v2
      - run: make install
      - run: make lint